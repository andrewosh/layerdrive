var test = require('tape')

var Layerdrive = require('..')
var Hyperdrive = require('hyperdrive')

test('read layer without writing')
test('read/write with one layer')
test('read/write with multiple layers')
